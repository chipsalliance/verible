dist: bionic

language: cpp

compiler:
  - gcc

addons:
  apt:
    packages:
      - bazel
      - g++-8

script:
  - bazel build --cxxopt='-std=c++17' //...
  - bazel test --cxxopt='-std=c++17' //...
